# Vercel Cron 每日優化（已於 Vercel Production/Preview 設定）
# CRON_SECRET — 隨機密鑰（Vercel Cron 帶 Authorization: Bearer …）
# GH_WORKFLOW_TOKEN — GitHub token（repo + workflow），觸發 GitHub Actions workflow_dispatch
#
# Vercel Cron 只 dispatch，唔直接 commit（避免同 GitHub Actions 雙寫衝突）。
# 無限期運作：全部啟用後進入輪播，每日仍更新，永不停止。末尾追加新功能會優先啟用。

# --- 登入 + 雲端同步（Universal）---
# 1) Vercel Marketplace 安裝 Clerk，再開 Google 登入
# 2) Clerk Dashboard → Restrictions → Allowlist 加入：
#      wanlokszevenus@gmail.com
#      whatnamecaniuseonjg99gle@gmail.com
# 3) Vercel Marketplace 安裝 Upstash Redis
# 4) 設定以下環境變數（Production + Preview），然後 Redeploy：
#
# VITE_CLERK_PUBLISHABLE_KEY — Clerk publishable key（前端，build 時注入）
# CLERK_SECRET_KEY — Clerk secret key（API 驗 token）
# UPSTASH_REDIS_REST_URL — Upstash REST URL
# UPSTASH_REDIS_REST_TOKEN — Upstash REST token
#
# （若用舊 KV 變數名亦可：KV_REST_API_URL / KV_REST_API_TOKEN）
