{
  "name": "trip-companion",
  "private": true,
  "version": "2.0.0",
  "description": "Multi-trip companion hub: Taipei, Tokyo, Thailand",
  "scripts": {
    "dev": "node server.js",
    "build:tokyo": "npm --prefix tokyo ci && npm --prefix tokyo run build",
    "build:universal": "node scripts/daily-optimization/ensure-feature-json.mjs --repair && npm --prefix universal ci && npm --prefix universal run build",
    "build": "npm run build:tokyo && npm run build:universal && node scripts/assemble.mjs",
    "start": "node server.js"
  },
  "dependencies": {
    "@clerk/backend": "^3.16.13",
    "@upstash/redis": "^1.38.3"
  }
}
